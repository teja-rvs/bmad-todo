#!/bin/bash -e

# If running the rails server then migrate existing database (check first two args to match default CMD)
if [ "$1" = "./bin/rails" ] && [ "$2" = "server" ]; then
  ./bin/rails db:migrate
fi

exec "${@}"
